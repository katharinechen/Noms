#!/bin/bash

if [ ! -e config.json ]; then
    cp sample/config.json .
fi
mongoimport --drop -d noms -c config config.json
mongoimport --drop -d noms -c recipe sample/recipe.json
mongoimport --drop -d noms -c secret_pair secret_pair.json
