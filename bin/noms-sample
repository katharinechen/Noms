#!/bin/bash

if [ -n "$1" ]; then
    host="-h $1"
else
    host=""
fi

mongoimport $host --drop -d noms -c config sample/config.json
mongoimport $host --drop -d noms -c recipe sample/recipe.json
mongoimport $host --drop -d noms -c secret_pair sample/secret_pair.json
