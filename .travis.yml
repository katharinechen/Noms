language: python
python:
  - "2.7"
# command to install dependencies
before_install: "pip install -U pip setuptools"
install: "pip install -r requirements.txt"
# command to run tests
script: py.test --cov-fail-under=100