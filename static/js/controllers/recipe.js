'use strict'; 

// controls the display of a single recipe
// need to write this 
// app.controller('Recipe', ['$scope', '$http', function($scope, $http) {
//     $http({method: 'GET', url: '/api/recipe/list'}).then(function(recipelist) {
//         $scope.recipes = recipelist.data; 
//     }); 
// }]); 